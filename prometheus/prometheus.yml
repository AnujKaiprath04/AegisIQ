global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "aegisiq-backend"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["aegisiq-backend:8000"]
        labels:
          environment: "production"
          app: "aegisiq-core"
